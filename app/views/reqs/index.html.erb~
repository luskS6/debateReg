<% if school_signed_in? %>

<% if current_school.role_id == 1 %>


<h1>Listing reqs</h1>
</br>
<% for school in @schools.sort{|a,b| a.sName <=> b.sName } %>
<%= link_to school.sName, reqs_path({:sView => school.id})%> |
<% end %>
<table>
  <tr>
    <th>School</th>
    <th>Diet</th>
    <th>Special</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @reqs.each do |req| %>
  <tr>
    <td><%= req.school_id %></td>
    <td><%= req.diet %></td>
    <td><%= req.special %></td>
    <td><%= link_to 'Show', req %></td>
    <td><%= link_to 'Edit', edit_req_path(req) %></td>
    <td><%= link_to 'Destroy', req, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Req', new_req_path %>
<% else %>
<script type ="text/javascript">
	window.location.href="/main/index"
</script>
<% end %>

<% end %>
