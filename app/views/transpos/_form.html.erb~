<%= form_for(@transpo) do |f| %>
  <% if @transpo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@transpo.errors.count, "error") %> prohibited this transpo from being saved:</h2>

      <ul>
      <% @transpo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p>This section is only if you need a lift to/from Garrison Train Station on either Friday/Saturday. If you do not need any transportation from us, go to the bottom of the page and press that thing labled "Submit". </p>

  <div class="field">
    Check if transportation is required on Friday"<br />
    <%= f.check_box :fpickup %>
  </div>
  <div class="field">
    "When are you arriving at Garrison on Friday?"<br />
    <%= f.time_select :fpickup_time %>
  </div>
  <div class="field">
    "How deep are you rolling?"<br />
    <%= f.number_field :fpickup_number %>
  </div>
  <div class="field">
    What about pick up on Saturday? (Also from Garrison Train Station)<br />
    <%= f.check_box :spickup %>
  </div>
  <div class="field">
    "Jeeves, I'd like to picked up at this time on Saturday"<br />
    <%= f.time_select :spickup_time %>
  </div>
  <div class="field">
    <%= f.label "With a party of..." %><br />
    <%= f.number_field :spickup_number %>
  </div>
  <div class="field">
    "In lieu of a transporter, I need a ride over to Garrison after the delectable activies on Saturday"<br />
    <%= f.check_box :sdropoff %>
  </div>
  <div class="field">
    <%= f.label "Transportation is a precise business." %><br />
    <%= f.time_select :sdropoff_time %>
  </div>
  <div class="field">
    <%= f.label "With my crew of..." %><br />
    <%= f.number_field :sdropoff_number %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
